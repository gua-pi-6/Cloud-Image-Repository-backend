package com.chen.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.chen.model.entity.PictureCollabState;

/**
 * 协同快照表访问层。
 *
 * <p>当前只使用 MyBatis-Plus 的基础 CRUD 能力，没有自定义 SQL。
 */
public interface PictureCollabStateMapper extends BaseMapper<PictureCollabState> {
}
